{
  "scripts": {
    "inw": "concurrently \"docker-compose up -d\" \"cd first && yarn dev\" \"cd first && php artisan serve\" \"cd second && yarn dev\""
  },
  "devDependencies": {
    "concurrently": "^9.0.1",
    "prettier": "^3.3.3",
    "prettier-plugin-tailwind": "^2.2.12"
  }
}